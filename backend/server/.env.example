# Server Configuration
PORT=3001

# Stellar / Soroban Configuration
STELLAR_NETWORK=TESTNET
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
STELLAR_HORIZON_URL=https://horizon-testnet.stellar.org
STELLAR_NETWORK_PASSPHRASE=Test SDF Network ; September 2015
FEE_PAYER_SECRET=S... (Stellar Secret Key — fee sponsor, never touches user keys)
PAYMENT_ROUTER_CONTRACT=C... (PaymentRouter Soroban contract ID)
REGISTRY_CONTRACT=C... (BLINKS Registry Soroban contract ID)

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/zaps?schema=public

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Worker / BullMQ
WORKER_CONCURRENCY=5
JOB_MAX_RETRIES=3
JOB_BACKOFF_DELAY_MS=1000

# Security
JWT_SECRET=super-secret-key
